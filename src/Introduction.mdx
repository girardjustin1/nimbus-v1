import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Introduction" />

export const protoUrl = (name) =>
  typeof window !== "undefined" && window.location.hostname === "localhost" ? `http://localhost:5190/${name}/` : `./${name}/`;

<div style={{ maxWidth: 900 }}>

# Nimbus Design System

The single source of truth for the **Nimbus** brand — design tokens, foundations,
reusable UI components, application patterns, and ready-to-use product screens, all
themed from the Nimbus Figma token export.

Built with **React 19 · TypeScript · Tailwind CSS v4 · React Aria · Recharts ·
Storybook 10 · Vite**.

<div style={{ display: "flex", gap: 12, flexWrap: "wrap", margin: "20px 0 28px" }}>
  <span style={{ background: "#DA6EA3", color: "#fff", padding: "6px 14px", borderRadius: 999, fontSize: 13, fontWeight: 600 }}>Pink primary · #DA6EA3</span>
  <span style={{ background: "#37B6B7", color: "#fff", padding: "6px 14px", borderRadius: 999, fontSize: 13, fontWeight: 600 }}>Teal secondary · #37B6B7</span>
  <span style={{ background: "#101828", color: "#fff", padding: "6px 14px", borderRadius: 999, fontSize: 13, fontWeight: 600 }}>Proxima Nova</span>
</div>

Everything here is generated from one theme, so the brand stays consistent from a
single button to a full application screen — in both light and dark modes.

<div style={{ background: "#F9FAFB", border: "1px solid #EAECF0", borderRadius: 18, padding: "20px 22px", margin: "8px 0 28px" }}>
  <div style={{ display: "flex", justifyContent: "space-between", alignItems: "baseline", flexWrap: "wrap", gap: 8 }}>
    <div style={{ fontSize: 12, fontWeight: 700, letterSpacing: 0.5, textTransform: "uppercase", color: "#475467" }}>Active prototypes</div>
    <div style={{ fontSize: 13, fontWeight: 600, color: "#101828" }}>September 22, 2026 · Round 1 review</div>
  </div>
  <div style={{ display: "flex", flexWrap: "wrap", gap: 16, marginTop: 14 }}>
  <div style={{ flex: "1 1 360px", display: "flex", flexDirection: "column", background: "#FFFFFF", border: "1px solid #EAECF0", borderTop: "4px solid #1F7F80", borderRadius: 14, padding: "18px 20px" }}>
    <span style={{ alignSelf: "flex-start", background: "#E6F6F6", color: "#1F7F80", fontSize: 12, fontWeight: 700, padding: "3px 10px", borderRadius: 999 }}>Deal Activation System</span>
    <div style={{ fontSize: 18, fontWeight: 700, color: "#101828", margin: "10px 0 6px" }}>Extended Targeting</div>
    <div style={{ fontSize: 14, lineHeight: 1.6, color: "#475467" }}>Set up a direct-sold campaign on one page, target it with the publisher's own keywords plus ad unit type and device language, manage keywords in a new library, and see at a glance whether every campaign is on pace to deliver.</div>
    <div style={{ fontSize: 13, color: "#667085", margin: "10px 0 16px" }}>Campaign setup · Targeting · Keyword library · Manage campaigns · Reporting — 17 screens</div>
    <div style={{ marginTop: "auto", display: "flex", alignItems: "center", flexWrap: "wrap", gap: 14 }}>
      <a href={protoUrl("das")} target="_top" style={{ background: "#1F7F80", color: "#FFFFFF", padding: "9px 16px", borderRadius: 999, fontSize: 14, fontWeight: 700, textDecoration: "none" }}>Open prototype →</a>
      <a href="./?path=/docs/deal-activation-system-overview--docs" target="_top" style={{ color: "#1F7F80", fontSize: 13, fontWeight: 600 }}>Storybook concepts</a>
    </div>
  </div>
  <div style={{ flex: "1 1 360px", display: "flex", flexDirection: "column", background: "#FFFFFF", border: "1px solid #EAECF0", borderTop: "4px solid #A94579", borderRadius: 14, padding: "18px 20px" }}>
    <span style={{ alignSelf: "flex-start", background: "#FCE7F1", color: "#A94579", fontSize: 12, fontWeight: 700, padding: "3px 10px", borderRadius: 999 }}>Performance Insights</span>
    <div style={{ fontSize: 18, fontWeight: 700, color: "#101828", margin: "10px 0 6px" }}>Reporting redesign</div>
    <div style={{ fontSize: 14, lineHeight: 1.6, color: "#475467" }}>Start from a template or saved query instead of a checkbox wall, ask for data as one readable sentence that only runs on demand, compare every number to the previous period, and drill from app to demand source in a pivot view.</div>
    <div style={{ fontSize: 13, color: "#667085", margin: "10px 0 16px" }}>Start page · Question bar · Explorer — 4 screens</div>
    <div style={{ marginTop: "auto", display: "flex", alignItems: "center", flexWrap: "wrap", gap: 14 }}>
      <a href={protoUrl("performance-insights")} target="_top" style={{ background: "#A94579", color: "#FFFFFF", padding: "9px 16px", borderRadius: 999, fontSize: 14, fontWeight: 700, textDecoration: "none" }}>Open prototype →</a>
      <a href="./?path=/docs/performance-insights-overview--docs" target="_top" style={{ color: "#A94579", fontSize: 13, fontWeight: 600 }}>Storybook concepts</a>
    </div>
  </div>
  </div>
  <div style={{ fontSize: 13, color: "#667085", marginTop: 14 }}>
    <strong style={{ color: "#344054" }}>Changelog</strong> · Sep 22, 2026: both prototypes now run as standalone, versioned apps (v1); Performance Insights concepts added · Sep 18, 2026: DAS kickoff, <em>Deal Activation System</em> category added.
  </div>
</div>

---

## How the library is organized

The sidebar is ordered top-to-bottom the way you'd build with it: start from the
brand primitives, move through components, then see them assembled into full screens.

### 🎨 Styles
The design foundations, rendered live from the Nimbus tokens.

**Color · Typography · Icons · Elevation · Shape · Logos**

### 🧱 Base Components
The core building blocks (19), each on React Aria for accessibility.

**Avatars · Badges · Badge Groups · Buttons · Button Group · Checkbox · Dropdown ·
File Upload · Form · Input · Multi Select · Progress Indicators · Radio Buttons ·
Select · Slider · Tags · Textarea · Toggle · Tooltip**

### 🧩 Application UI
Richer, composed patterns (23) for building product interfaces.

**App Navigation – Sidebar · Alerts · Breadcrumbs · Carousel · Charts · Code Snippet ·
Command Menu · Date Picker · Dividers · Empty State · File Upload · Filter Bars ·
Loading Indicator · Metrics · Modal · Notifications · Pagination · Pie Charts ·
Progress Steps · Radar Charts · Slideout Menu · Table · Tabs**

### 🔐 Account Login
Auth page templates (4): **Sign up · Log in · Forgot Password · Verify Email**.

### 🖥️ App Screens
Full Nimbus product screens (17) assembled entirely from the system — each with
multiple states/stories where relevant.

| Screen | What it covers |
| --- | --- |
| Key Metrics Dashboard | Nimbus Exchange metric cards + sparkline charts |
| Performance Insights | Reporting query builder (table / line / bar / pie states) |
| Saved Queries | Saved reports, query builder & save-query modal |
| Nimbus+ Reporting | Nimbus+ demand upsell filter panel + modal |
| Nimbus Benchmarks | Nimbus-vs-your-app comparison, config & leaderboard |
| Nimbus+ Payments | Publisher statements, payouts & completed payments |
| Nimbus+ Payouts | Internal payouts admin (submit / resubmit states) |
| Manage Active Demand | App-Ads.txt flow + integrated demand partners |
| Manage Assets | Add asset · create campaign · view campaigns |
| View All Campaigns | Wide campaigns table + add-asset form |
| Campaign Setup | 8-step campaign creation wizard |
| Deal Activation / DAS – Billing Rates | Deal activation setup & billing rates |
| Billing Setup | Billing configuration + modals |
| Add DocuSign Contracts | Contract signing flow |
| SDK & Documentation | iOS / Android SDK cards |
| Ad Blocking · Ad Blocking – Blocklists | Blocked categories / advertisers / apps |

---

## Brand & tokens

The theme comes from the Nimbus design tokens in `reference/styles/` and is applied in
`src/styles/theme.css`:

- **Brand** — pink primary (`#DA6EA3` / `--color-brand-500`) with teal (`#37B6B7`) as the secondary / `_alt` accent.
- **Neutrals** — a warm gray scale for surfaces, text, and borders.
- **Typography** — Proxima Nova on the Nimbus type scale (`text-md` 15px, display sizes up to 72px).
- **Radius & spacing** — Nimbus token values (e.g. `radius-md` 8px).

Reuse the theme classes (`bg-brand-solid`, `text-primary`, `border-secondary`,
`text-md`, `radius-md`, …) and new work inherits the Nimbus brand for free.

## Getting started

```bash
npm install              # install dependencies
npm run storybook        # launch this library at http://localhost:6006
npm run build-storybook  # static build → ./storybook-static
```

Add a component or screen with a matching `*.stories.tsx` and it appears in the correct
category automatically. Every push to `main` redeploys the live Storybook to GitHub Pages:

**→ https://girardjustin1.github.io/nimbus-v1/**

</div>
