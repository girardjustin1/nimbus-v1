# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-prototypes
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env

*storybook.log
storybook-static

# Design-source exports (Figma PNGs / raw token dumps) — kept local, not versioned.
# Values extracted from these live in src/ (e.g. color-scales.data.ts).
reference/screens/
reference/screens - batch 2/
reference/screens - batch 3/
reference/colors/

# DAS project sources (internal PDFs, staging screenshots, extracted notes) — local only.
reference/das-system/
